@using BlazorComponents.Monaco
@using Microsoft.AspNetCore.Blazor.Components
<div class="row">
	<div class="col">
		<div id="@EditorModel.Id" width="@Width" height="@Height" style="width:800px;height:600px;border:1px solid grey"></div>
	</div>
</div>
@functions {

[Parameter]
EditorModel EditorModel { get; set; }

[Parameter]
int Width { get; set; } = 400;

[Parameter]
int Height { get; set; } = 200;

protected override void OnAfterRender()
{
	MonacoInterop.InitializeEditor(EditorModel);
}

public void UpdateEditor(EditorModel editorOptions)
{
	editorOptions = MonacoInterop.UpdateEditor(editorOptions);
}
}
